tmux start-server\; has-session -t $1 2>/dev/null

if [ "$?" -eq 1 ]; then
  SESSION_NAME=$1 sh armux/$1
fi

if [ -z "$TMUX" ]; then
  tmux -u attach-session -t $1
else
  tmux -u switch-client -t $1
fi
